#import "./fragmentRoom.graphql"

query getRoom($id: String!){
    getRoom(id: $id){
        status
        errors {
            field
            message
        }
        room {
            ...Room
        }
    }
}
