#import "./fragmentChat.graphql"

mutation CreateRoomMutation($bookId: String!){
    createChat(bookId: $bookId) {
        status
        errors {
            field
            message
        }
        chat {
            ...Chat
        }
    }
}
