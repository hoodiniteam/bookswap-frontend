#import "./fragmentChat.graphql"

mutation InitSwap($roomId: String!, $chatId: String!, $recipientId: String!){
    initSwap(bookId: $roomId, chatId: $chatId, recipientId: $recipientId) {
        status
        errors {
            field
            message
        }
        chat {
            ...Chat
        }
    }
}
