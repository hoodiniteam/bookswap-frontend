#import "./fragmentUser.graphql"

mutation ClearNotifications {
    clearNotifications {
        status
        errors {
            field
            message
        }
        user {
            ...User
        }
    }
}
