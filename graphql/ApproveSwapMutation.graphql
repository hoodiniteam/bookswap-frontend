#import "./fragmentRoom.graphql"

mutation ApproveSwap($swapId: String!){
    approveSwap(swapId: $swapId) {
        status
        errors {
            field
            message
        }
        room {
            ...Room
        }
    }
}
