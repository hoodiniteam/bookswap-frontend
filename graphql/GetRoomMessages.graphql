query getRoomMessages($id: String!){
    getRoomMessages(id: $id){
        status
        errors {
            field
            message
        }
        room {
            id
            messages {
                createdAt
                message
                userId
                isRead
            }
        }
    }
}
